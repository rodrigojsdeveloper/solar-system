import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "row",
    backgroundColor: "#090909",
    paddingLeft: 20,
    paddingTop: 20,
    overflowX: "scroll",
  }
});

export { styles };
